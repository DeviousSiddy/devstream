version: '3.8'

services:
  devstream:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./server:/app/server
    environment:
      - PORT=3000
    restart: unless-stopped
